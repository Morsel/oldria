<% title @event.title %>

<%= @event.start_at.to_formatted_s(:short) %> - <%= @event.end_at.to_formatted_s(:short) if @event.end_at %>

<%= simple_format(@event.description) %>

<%= simple_format(@event.location) %>

<%= link_to "Back to calendars", restaurant_calendars_path(:restaurant_id => @restaurant) %>