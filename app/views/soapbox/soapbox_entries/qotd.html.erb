<% title "Soapbox", false %>

<section id="questions-list">
  <div id="questions-list-feed-qotd">
    <h1><%= params[:view_all].present? ? "Questions of the Day" : "Previous Featured Questions of the Day" %></h1>
    <%= render :partial => 'qotd_list', :locals => {:questions => @featured_items} %>
    <%= will_paginate(@featured_items) %>
  </div><!-- /recent-trends -->
</section><!-- / recent-questions -->
