<% title "Editing pages" %>

<% form_for([:admin, @page]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= button_tag 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', [:admin, @page] %> |
<%= link_to 'Back', admin_pages_path %>