<label for="profile_question_restaurant_roles">For which pages?<abbr title="required">*</abbr></label>

<%= f.input :responders, :as => :check_boxes,
      :label => "&nbsp;",
      :collection => RestaurantFeaturePage.all,
      :required => false,
      :wrapper_html => { :id => "profile_question_restaurant_roles_#{category.gsub(/ /, "_") rescue nil}_input" } %>
