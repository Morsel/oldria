
<%= semantic_form_for @user_session do |f| %>
    <%= f.inputs do %>
        <%= f.input :username, :hint => 'You can use your <strong>username</strong> or <strong>email address.</strong>'.html_safe %>
        <%= f.input :password %>
        <p class="clear forgot"><%= link_to "Forgot your password?", (mediafeed? ? mediafeed_forgot_password_path : new_password_reset_path) %></p>
        <li>
            <label><%= f.check_box :remember_me %> Remember me?</label>
        </li>

        <p><%= button_tag "Login", :class => 'positive', :name => 'submit' %></p>
    <% end %>
<% end %>

<div class="fb">
    Or login with Facebook:
    <p><%= fb_login_link fb_login_path %></p>
</div>
