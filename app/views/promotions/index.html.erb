<% title "Newsfeed: #{@restaurant.name}" %>

<div id="promotion_archive">
    <%= render @promotions %>
    
    <% if @past_promotions.present? -%>
    <div id="past">
        <h2>Past News</h2>
        <%= render @past_promotions %>
    </div>
    <% end -%>
</div>
