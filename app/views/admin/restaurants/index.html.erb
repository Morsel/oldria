<% title "Restaurants" %>

<%= table_for(@restaurants) do |t| %>
	<%= t.data :name,
             :account_type,
             :city,
             :state,
             :manager,
             :media_contact,
             :metropolitan_area,
             :cuisine,             
             :actions => [:edit, :destroy],
             :action_prefix => :admin %>
<% end -%>

