<section id="trend-questions-found">
  <div id="all-questions-feed">
    <h1>Trend Questions</h1>
    <ul>
        <% @trend_questions.each do |question| %>
        <li>
            <% link_to soapbox_soapbox_entry_path(question.soapbox_entry) do %>
                <% date = question.scheduled_at %>
                <time datetime="<%= date.try(:strftime,'%Y-%m-%d') %>"><span><%= date.try(:strftime, "%b").upcase %></span><%= date.try(:strftime, "%e") %></time>
                <span class="title"><%= question.display_message || question.subject %></span>
                <span class="reply-count"><span><%= count = question.soapbox_comment_count %></span><%= count > 1 ? "replies" : "reply" %></span>
            <% end %>
        </li>
        <% end %>
    </ul>   
  </div><!-- /recent-trends -->
</section><!-- / recent-questions -->

<section id="qotds-found">
  <div id="all-questions-feed-qotd">
    <h1>Questions of the Day</h1>
    <ul>
        <% @qotds.each do |question| %>
        <li>
            <% link_to soapbox_soapbox_entry_path(question.soapbox_entry) do %>
                <% date = question.scheduled_at %>
                <time datetime="<%= date.try(:strftime,'%Y-%m-%d') %>"><span><%= date.try(:strftime, "%b").upcase %></span><%= date.try(:strftime, "%e") %></time>
                <span class="title"><%= question.display_message || question.message %></span>
                <span class="reply-count"><span><%= count = question.soapbox_comment_count %></span><%= count > 1 ? "replies" : "reply" %></span>
            <% end %>
        </li>
        <% end %>
    </ul>
  </div><!-- /recent-trends -->
</section><!-- / recent-questions -->