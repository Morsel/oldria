<div class="identity">
  <% if user.avatar? %>
    <div class="avatar">
      <%= image_tag(user.avatar.url(:thumb)) %>
    </div>
  <% end %>

  <% employment ||= user.primary_employment %>

  <%= identity_details_list(employment, highlight_restaurant) %>
</div>
