<% title "Page" %>

<p>
	<strong>Title:</strong>
	<%=h @page.title %>
</p>
<p>
	<strong>Slug:</strong>
	<%=h @page.slug %>
</p>
<p>
	<strong>Content:</strong>
	<%=h @page.content %>
</p>

<p>
	<%= link_to "Edit", edit_admin_hq_page_path(@page) %> |
	<%= link_to "Destroy", @page, :confirm => 'Are you sure?', :method => :delete %> |
	<%= link_to "View All", admin_hq_pages_path %>
</p>
