<% title "On the Menu Keywords" %>

<p class="clearfix"><%= link_to 'New OTM Keyword', new_admin_otm_keyword_path, :class => 'button positive' %></p>

<div>
    <% @categories.each do |category, keywords| -%>
    <h2><%= category %></h2>
    <% table_for(keywords) do |t| %>
        <% t.data :actions => [:edit, :destroy], :action_prefix => 'admin' do
             t.cell(:name)
             t.cell :count, :heading => '', :cell_html => {:class => 'actions count'} do |keyword| keyword.menu_items.count    
              end     
        end %>
        <% t.data   %>
    <% end %>
    <% end -%>
</div>

