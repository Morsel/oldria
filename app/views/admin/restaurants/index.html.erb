<% title "Restaurants" %>

<% table_for(@restaurants) do |t| %>
	<%= t.data :name,
             :account_type,
             :city,
             :state,
             :manager,
             :metropolitan_area,
             :cuisine,
             :media_contact,
             :actions => [:edit, :destroy],
             :action_prefix => :admin %>
<% end -%>

