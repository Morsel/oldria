<nav id="topic_nav" class="topic-nav clearfix">
    <%= link_to "<span>Next:</span> #{truncate(next_topic.title, :length => 20, :omission=>'&hellip;')}", link_for_restaurant_chapters(:restaurant_id => @restaurant.id, :id => next_topic.id, :page_id => @page.try(:id)), :class => "next" if next_topic %>
    <%= link_to "All Topics", link_for_restaurant_topics(:restaurant_id => @restaurant.id, :page_id => @page.try(:id)), :class => "all-topics" %>
    <%= link_to "<span>Prev:</span> #{truncate(prev_topic.title, :length => 20, :omission=>'&hellip;')}", link_for_restaurant_chapters(:restaurant_id => @restaurant.id, :id => prev_topic.id, :page_id => @page.try(:id)), :class => "prev" if prev_topic %>
</nav>